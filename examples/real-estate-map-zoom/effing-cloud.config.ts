import { defineConfig } from "effing-cloud";

export default defineConfig({
  project: "real-estate-map-zoom",
  images: "app/images/*.fn.tsx",
  annies: "app/annies/*.fn.tsx",
  effies: "app/effies/*.fn.tsx",
});
