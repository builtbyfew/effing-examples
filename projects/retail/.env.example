# Required: secret for signing fn URL segments
SECRET_KEY=s3cr3t-k3y-goes-here

# Optional in dev — `effing dev` defaults BASE_URL to the dev server's own
# address and FFS_BASE_URL to the auto-spawned local sidecar. Set them only
# for production (BASE_URL) or a remote FFS server (FFS_*).
# BASE_URL=http://localhost:3839
# FFS_BASE_URL=https://your-ffs.example.com
# FFS_API_KEY=ffs-4p1-k3y-goes-here
